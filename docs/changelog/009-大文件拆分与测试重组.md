# 009 - 大文件拆分与测试重组

- **类型**: refactor
- **日期**: 2026-04-14
- **Commit**: `d3a1669`

## 问题描述

多个源文件超过 CLAUDE.md 规定的 1000 行上限，不利于维护和理解。同时 `test_all.cpp` 中直接实现了测试函数，违反测试组织规范。

## 实现方案

### 源文件拆分
- **MacSearchBridge.mm** (949→671 行)：提取内容索引相关代码到 `MacSearchBridge+Content.mm`，使用 ObjC Category 模式，共享实例变量通过 `MacSearchBridge_Internal.h` 声明
- **SearchEngine.cpp** (802→603 行)：提取持久化 I/O 到 `SearchEnginePersistence.cpp`

### 代码清理
- 移除重复的 `fullPathForRecord()`，将 `makeFullPath()` 改为 public
- 提取 `test_all.cpp` 中的所有测试实现到独立的 `tests/*.h` 文件

### 稳定性修复
- WAL CRC32 完整性校验
- 批量 fsync
- Scanner cancel 支持
- 析构安全
- Compaction 重映射

## 影响的文件

- `MacEverything/Bridge/MacSearchBridge.mm`
- `MacEverything/Bridge/MacSearchBridge+Content.mm`（新增）
- `MacEverything/Bridge/MacSearchBridge_Internal.h`（新增）
- `MacEverything/Core/SearchEngine.cpp` / `.h`
- `MacEverything/Core/SearchEnginePersistence.cpp`（新增）
- `tests/test_all.cpp`
- `MacEverything.xcodeproj/project.pbxproj`

## 测试覆盖

242 个测试通过。Xcode Release 构建成功。
