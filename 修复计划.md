# 修复计划

## 1. 目标与原则

- **根因修复优先**：不做表面补丁，从设计层面消除问题再发生的可能
- **保持复杂度可控**：每项修复应减少或不增加系统复杂度
- **平衡内存与计算成本**：不以大幅增加一方来换取另一方
- **最小改动原则**：尽量在现有抽象层上修复，避免大规模重构

---

## 2. 分批实施路线

### P0 — 必须立即修复（影响数据完整性和核心体验）

---

#### P0-1: 重构 `prepareForTermination` 终止流程

**对应问题**：C-01

**修复动作**：
1. 将终止流程改为「优先保存增量 → 降级放弃全量」策略：
   - 第一步（<500ms）：flush 当前 WAL 缓冲（已有数据的 `fflush` + `fsync`），这是最低成本的持久化
   - 第二步（<2s）：尝试 compact，但设置硬超时（2 秒），超时则中止 compact 并保留 WAL
   - 移除或缩短 content indexing 的 5 秒 semaphore wait（改为 1 秒，因为 WAL 已保证增量安全）
2. 将 compact 从 main thread 移到后台线程执行，main thread 仅做 `dispatch_sync` 等待（带超时）
3. 总耗时硬约束：**不超过 3 秒**

**根因修复策略**：当前设计假设"终止时必须完成全量 compact"，这是错误的。WAL 的存在就是为了 crash recovery — 只要 WAL 完整 flush，下次启动通过 WAL replay 即可恢复，compact 是优化而非必须。

**验收标准**：
- 终止流程在任何状态下（scanning、content indexing、idle）均在 3 秒内完成
- 杀进程后重启，索引数据不丢失（WAL replay 恢复）
- 添加终止流程耗时的集成测试

---

#### P0-2: Background sync 改用 engine 原子替换

**对应问题**：H-01

**修复动作**：
1. `backgroundSyncEngine`（`MacSearchBridge.mm:380-450`）中，将 `engine->loadRecords(freshRecords)` 改为：
   - 创建新 `SearchEngine` 实例
   - 在新实例上调用 `loadRecords()`（无锁竞争，因为新实例无人持有引用）
   - 通过 `[self setEngine:newEngine]` 原子替换（与 `startScanFrom:` 使用相同模式）
2. 旧 engine 的 `shared_ptr` 在最后一个查询释放引用后自动析构

**根因修复策略**：问题根因是"在活跃 engine 上做 exclusive 操作"。解决方式是从 `startScanFrom:` 学习，采用构建新实例 → 原子替换的模式，彻底消除 query 阻塞。

**验收标准**：
- background sync 期间搜索响应无中断
- 并发测试：在 background sync 执行中持续发起 query，所有 query 在 100ms 内返回
- 内存峰值不超过原方案的 2x（短暂存在两个 engine 实例）

---

#### P0-3: Instance lock 失败时阻止写入

**对应问题**：H-02

**修复动作**：
1. 当 `tryLock()` 失败时：
   - 将 engine 置为只读模式（允许 query，禁止 addRecord/removeByPath/compact）
   - 不 attach WAL（避免双写）
   - 通过 `onScanProgress` callback 向 UI 报告 "另一个 MacEverything 实例正在运行，当前为只读模式"
2. 在 `MacSearchBridge` 中添加 `_readOnly` atomic flag，所有写操作入口检查该 flag

**根因修复策略**：当前"warn-and-continue"模式的根因是缺乏只读降级路径。添加只读模式后，lock 失败变为安全的降级行为而非潜在的数据腐败。

**验收标准**：
- 同时启动两个实例，第二个实例显示只读提示
- 只读模式下搜索功能正常
- 只读模式下不写入任何 WAL 或索引文件
- 添加 InstanceLock 失败场景的测试

---

### P1 — 应当尽快修复（影响性能和可维护性）

---

#### P1-1: `saveToFile()` 减少 lock 持有时间

**对应问题**：M-01

**修复动作**：
1. 在 `shared_lock` 内仅做数据 snapshot（复制 records vector、PathTable、trigram index 到局部变量）
2. 释放 lock 后执行文件写入
3. snapshot 可通过 `records_` 的 copy-on-write 或直接 copy 实现

**根因修复策略**：lock 持有时间应与数据访问时间成正比，而非 I/O 时间。snapshot + unlock + write 模式是标准的解耦方式。

**验收标准**：
- saveToFile 期间 addRecord/removeByPath 不被阻塞
- 保存前后数据一致性不变（对比 save + load round-trip）
- 添加并发 save + write 的测试

**风险与回滚**：snapshot 会短暂增加约 1x 内存占用。对于百万级记录（约 200MB engine），峰值内存增加 ~200MB。这是「内存换时间」的典型 tradeoff，但持有时间从「秒级 I/O」缩短到「毫秒级 copy」，收益显著。如果内存增量不可接受，可改为分块 snapshot + 分块写入。

---

#### P1-2: 建立 Bridge 层测试基础设施

**对应问题**：H-03

**修复动作**：
1. 创建 `tests/test_bridge.h`，在 test_all.cpp 中 include
2. 编写以下核心测试用例：
   - **Shutdown 协调**：模拟 scanning + content indexing 状态下调用 prepareForTermination，验证不死锁且在超时内完成
   - **Engine 热替换**：并发 query + setEngine，验证无 crash 和数据一致性
   - **只读模式**（P0-3 完成后）：instance lock 失败后验证写操作被拒绝
3. 由于 Bridge 层依赖 ObjC++ runtime，测试需通过 `clang++ -ObjC++` 编译或在 Xcode test target 中运行

**根因修复策略**：Bridge 层是 bug 密度最高的区域，缺乏测试的根因是「测试基础设施不存在」。建立基础设施后，后续每个 Bridge 层修复都有回归守护。

**验收标准**：
- 至少 5 个 Bridge 层测试用例通过
- 测试可在 `make test-fast` 中运行
- shutdown 测试验证 3 秒超时约束（P0-1）

---

#### P1-3: `setEngine:` 避免在 main thread 上阻塞等锁

**对应问题**：M-02

**修复动作**：
1. 将 `setEngine:` 中的 engine 替换操作从 main thread 移到专用的 `_mutationQueue` 上
2. 替换完成后通过 `dispatch_async(dispatch_get_main_queue(), ...)` 通知 UI 更新
3. 或者：将 `_engineMutex` 改为 `std::atomic<std::shared_ptr<SearchEngine>>`（C++20 支持），消除 lock 竞争

**根因修复策略**：main thread 不应参与任何可能阻塞的 lock 竞争。engine 替换是低频操作（scan/sync 完成时），完全可以在后台队列完成。

**验收标准**：
- 用 Instruments（Time Profiler）验证 main thread 无 `_engineMutex` 等待
- engine 替换前后 UI 更新正常

---

### P2 — 可择机修复（小幅优化和代码质量）

---

#### P2-1: ContentIndexPersistence WAL 改用内存跟踪大小

**对应问题**：M-03

**修复动作**：
1. 在 `ContentIndexPersistence` 中添加 `currentSize_` 成员变量，初始化时从 `fstat` 获取
2. 每次 append 后累加写入字节数
3. 用 `currentSize_` 替换每次 append 中的 `fstat()` 调用
4. 参考 `IndexWAL` 的现有实现

**验收标准**：
- 功能不变（WAL 文件大小超限时正确触发 compact）
- `strace`/`dtrace` 确认 append 时无 `fstat` syscall

---

#### P2-2: `query()` 方法拆分

**对应问题**：M-04

**修复动作**：
1. 将 `query()` 拆分为以下 private 方法：
   - `extractQueryTrigrams(query) -> vector<uint32_t>`
   - `findCandidatesByTrigram(trigrams, candidates) -> void`（Phase 1）
   - `filterByPathParallel(candidates, lowerQuery, generation) -> vector<Result>`（Phase 2）
   - `rankAndSort(results, query, maxResults) -> vector<FileRecord>`
2. `query()` 主体变为 4 个阶段调用的协调者

**验收标准**：
- 所有现有 query 测试通过
- `query()` 主体不超过 50 行
- 性能不退化（benchmark 对比）

---

#### P2-3: query 候选位图 thread-local 复用

**对应问题**：M-06

**修复动作**：
1. 将 `vector<bool> candidates` 改为 `thread_local static vector<bool>` 
2. 每次 query 开始时 `resize` + `fill(false)` 而非重新分配
3. 类似 `ContentIndex::extractTrigrams` 中 thread_local bitmap 的已有模式

**验收标准**：
- 高频 query 场景下内存分配次数显著减少（通过 allocator profiling 验证）
- 并发 query 测试通过（thread_local 保证线程安全）

---

#### P2-4: DirectoryScanner dedup 锁优化

**对应问题**：M-05

**修复动作**：
1. 将 per-thread dedup 改为每个工作线程维护局部 `unordered_set<InodeKey>` 
2. scan 完成后合并各线程的 dedup set
3. 或者使用 `std::shared_mutex`：读检查用 shared_lock（高频），写插入用 unique_lock（低频，仅新目录）

**验收标准**：
- 目录密集文件系统上扫描时间不退化
- dedup 正确性不变（相同 inode 不重复遍历）

---

#### P2-5: `removeFile` 使用 `lower_bound`

**对应问题**：L-02

**修复动作**：
1. 在 `ContentIndex.cpp:293-298` 和 `337-339`，将 `std::find` 改为 `std::lower_bound`
2. 查找后验证找到的元素确实匹配（`lower_bound` 返回的是"不小于"的位置）

**验收标准**：
- 现有 ContentIndex 测试通过
- posting list 较长时（>1000 条）删除性能提升（micro benchmark）

---

#### P2-6: `StringUtils::toLower` 单元测试

**对应问题**：L-04

**修复动作**：
1. 创建 `tests/test_string_utils.h`，添加以下测试用例：
   - ASCII 基础："Hello" → "hello"
   - 德语 umlaut："Über" → "über"  
   - 中文（不变）："你好" → "你好"
   - 混合："File名_Über.txt" → "file名_über.txt"
   - 空字符串
   - 纯数字/符号

**验收标准**：
- 所有用例通过
- 在 test_all.cpp 中 include 并可通过 `make test-fast` 运行

---

#### P2-7: `exclusionPaths_` 线程安全

**对应问题**：L-01

**修复动作**：
1. 在 `setExclusionPaths()` 中添加 `std::lock_guard` 保护
2. 在 `fseventsCallback` 读取时使用相同锁
3. 或者将 `exclusionPaths_` 改为 `std::atomic<std::shared_ptr<vector<string>>>`，实现 lock-free 读

**验收标准**：
- TSan (Thread Sanitizer) 无告警
- 功能不变

---

#### P2-8: `pathEndsWithApp` 参数统一

**对应问题**：L-05

**修复动作**：
1. 统一两处调用均使用 `fileName`（语义更清晰，且两处上下文都有 fileName 可用）

**验收标准**：
- .app bundle 检测行为不变

---

## 3. 风险与回滚

### P0 风险控制

| 修复项 | 主要风险 | 回滚方案 |
|--------|----------|----------|
| P0-1 终止流程 | 缩短超时可能导致极端情况下 compact 未完成 | WAL 保证增量安全，未完成的 compact 在下次启动时通过 replay 恢复 |
| P0-2 engine 原子替换 | 短暂 2x 内存峰值 | 可配置回退到原方案（feature flag），但预计不需要 |
| P0-3 只读模式 | 用户可能困惑于只读提示 | 提供清晰的 UI 提示和"关闭另一个实例"操作引导 |

### P1 风险控制

| 修复项 | 主要风险 | 回滚方案 |
|--------|----------|----------|
| P1-1 saveToFile snapshot | 短暂 1x 内存峰值 | 可改为增量写入（仅写 dirty records），但复杂度更高 |
| P1-2 Bridge 测试 | 测试本身引入新依赖 | 使用现有 test_helpers.h 框架，不引入外部依赖 |
| P1-3 setEngine 移出 main thread | UI 更新时序可能变化 | 保留 main queue dispatch 用于 UI 通知，仅 lock 操作移到后台 |

### P2 风险控制

P2 项均为局部优化，风险极低。每项修改影响范围不超过 1-2 个文件，可通过现有测试验证。

---

## 4. 实施依赖关系

```
P0-1 (终止流程) ─── 无依赖，可立即开始
P0-2 (engine 替换) ── 无依赖，可立即开始
P0-3 (只读模式) ──── 无依赖，可立即开始
       │
       ▼
P1-2 (Bridge 测试) ── 依赖 P0-1, P0-3 完成后编写对应测试
P1-1 (saveToFile) ─── 无依赖，可与 P0 并行
P1-3 (setEngine) ──── 无依赖，可与 P0 并行

P2-* ──────────────── 均无依赖，可按优先级顺序实施
```

P0 三项可并行开发。P1-2 需要等 P0-1 和 P0-3 完成后才能编写完整的测试用例。
