# 修复计划评估（第二轮）

## 1. 结论

- **有条件通过**
- 14 项修复计划中，12 项通过评估可按原计划执行，1 项需调整方案（P1-1），1 项应从当前修复周期移除（P2-4）。P1-3 已按第一轮建议明确选定 `_mutationQueue` 方案，**通过**。
- 所有 15 个审计问题均已在源码中逐一验证为真实存在的问题，无虚报。
- 修复计划整体遵循「根因修复优先」原则，未发现表面补丁行为。

---

## 2. 逐项评估

---

### P0-1: 重构 `prepareForTermination` 终止流程

**问题真实性**：**真实，已验证**
- `MacSearchBridge.mm:475-509`：终止流程为同步阻塞式。先设置 `_shuttingDown` flag，再等待 content indexing 完成（semaphore wait 最多 5 秒，line 492），然后串行执行 `persistence->compact(lastEventId)` 和 `contentPersistence->compact()`。
- 最坏情况总耗时可达 7-10 秒，超出 macOS `applicationWillTerminate:` 的 ~5 秒系统超时。
- 超时后进程被 SIGKILL，WAL 可能未完成 flush，导致下次启动需全量重建。

**严重程度判断**：**P0 正确** — 直接影响数据持久化可靠性，用户可观察到的数据丢失风险。

**修复必要性**：**必要**
- WAL 的设计目的就是 crash recovery，但当前终止流程将 compact（慢操作）置于 WAL flush（快操作）之前的优先级，本末倒置。

**方案评价**：
- 「优先 WAL flush → 降级放弃 compact」策略正确。WAL flush 仅需 `fflush` + `fsync`（毫秒级），compact 是优化而非必须。
- 将 content indexing semaphore wait 从 5 秒缩短到 1 秒合理 — WAL 已保证增量安全，无需等待 content indexing 完成。
- 总耗时硬约束 3 秒合理，与 macOS 系统超时留有余量。
- 已验证 WAL replay 路径存在：`IndexPersistence` 的 `load()` 方法会读取并 replay WAL 条目，`ContentIndexPersistence::load()` 同样有 WAL replay 逻辑。
- **无过度修复风险**：改动范围限于 `prepareForTermination` 方法内部，不影响其他流程。

**结论**：**通过**

---

### P0-2: Background sync 改用 engine 原子替换

**问题真实性**：**真实，已验证**
- `MacSearchBridge.mm:417`：`backgroundSyncEngine` 的 fallback 路径调用 `engine->loadRecords(std::move(freshRecords))`，在活跃 engine 上执行。
- `SearchEngine.cpp:77-143`：`loadRecords()` 获取 `unique_lock(mutex_)`，持有期间执行多线程小写转换、路径 intern、trigram 索引重建，耗时可达数百毫秒到秒级。
- 期间所有 `query()` 调用因 `shared_lock` 竞争被阻塞。

**严重程度判断**：**P0 正确** — 用户在后台同步期间体验到搜索无响应，属于核心交互体验问题。

**修复必要性**：**必要**
- `startScanFrom:`（line 190-198）已使用正确模式：构建新 engine -> `setEngine:` 原子替换。`backgroundSyncEngine` 的 fallback 路径未对齐到此模式，是实现不一致。

**方案评价**：
- 对齐到 `startScanFrom:` 已有模式，复用已验证的代码路径，改动范围小。
- 短暂 2x 内存峰值：旧 engine（~200MB）在最后一个 `shared_ptr` 引用释放后自动析构，通常毫秒级。该行为已存在于 `startScanFrom:` 路径中，不是新增开销。
- **无过度修复风险**：本质是将 fallback 路径对齐到已有正确模式。

**结论**：**通过**

---

### P0-3: Instance lock 失败时阻止写入

**问题真实性**：**真实，已验证**
- `MacSearchBridge.mm:240-242`：`tryLock()` 失败后仅打 `LOG_WARN` 日志，无任何保护措施，继续正常运行包括写 WAL。
- `InstanceLock.cpp`：使用 POSIX `flock(fd_, LOCK_EX | LOCK_NB)` 进行 advisory lock，lock 失败意味着另一个实例已在运行。
- 两个进程同时写同一 WAL 文件会导致数据交错和语义状态不一致。

**严重程度判断**：**P0 正确** — 数据完整性风险，可导致索引腐败。

**修复必要性**：**必要**
- 当前 "warn-and-continue" 是明显的设计缺陷，缺乏降级路径。

**方案评价**：
- 只读模式（允许 query，禁止 addRecord/removeByPath/compact）是系统性修复，不是表面补丁。
- 不 attach WAL 避免双写，从根本上消除数据腐败风险。
- 通过 `onScanProgress` callback 通知 UI 是利用现有机制，不引入新复杂度。
- `_readOnly` atomic flag + 写操作入口检查的模式简洁、可理解。
- `MacSearchBridge_Internal.h` 中目前无 `_readOnly` 字段，需新增，但这是必要的最小改动。
- **无过度修复风险**：P0-3 是防御性降级路径，不是功能扩展。

**结论**：**通过**

---

### P1-1: `saveToFile()` 减少 lock 持有时间

**问题真实性**：**真实，已验证**
- `SearchEnginePersistence.cpp:64-128`：`saveToFile()` 在 line 65 获取 `shared_lock(mutex_)` 后，持有直到函数结束（line ~128），期间执行全量文件写入（header、逐条 record 写入、fsync、close、rename）。
- `shared_lock` 虽然不阻塞 query（同为 shared_lock），但阻塞所有写操作（addRecord/removeByPath 需要 unique_lock）。
- 大型索引（百万级记录）的 I/O 写入可达数百毫秒到秒级。

**严重程度判断**：**P1 正确** — 影响写入性能但不影响查询，非立即故障。

**修复必要性**：**必要**
- lock 持有时间与 I/O 时间耦合是设计缺陷，应解耦为「数据访问时间」。

**方案评价**：
- **原计划（full snapshot）存在问题**：第一轮评估正确指出，全量 snapshot（records、PathTable、trigram index）约 200MB 额外内存，持续整个 I/O 写入时间，违反资源平衡约束。
- **第一轮建议的方案 A（分批次 lock）**：将 `shared_lock` 缩小到仅覆盖分批次数据读取（如每 1000 条），读取后释放 lock，写入磁盘。lock 持有时间从秒级降到毫秒级，内存增量接近零。但需注意：分批次写入期间数据可能变化，文件一致性需通过 write-to-temp + atomic rename 保证（当前已有此模式）。
- **第一轮建议的方案 B（最小 snapshot）**：仅 snapshot `records_`、`pathTable_`、`pathIndices_` 三者（排除 trigram index 等非持久化数据），减少 snapshot 大小到约 50-100MB。持续时间仍等于 I/O 写入时间，但内存开销降低一半以上。
- **建议采用方案 A**，原因：内存增量最小，lock 持有时间最短，且 `saveToFile` 已使用 temp 文件 + rename 模式保证原子性。分批次读取期间若数据变化，worst case 是保存了一个混合了新旧数据的 snapshot，但由于 saveToFile 本身是后台优化操作（WAL 保证增量安全），这种不一致可以接受。

**结论**：**有条件通过** — 需将 full snapshot 方案替换为分批次 lock 方案（方案 A），或限定 snapshot 范围为最小持久化所需数据并明确内存上界（方案 B）。

---

### P1-2: 建立 Bridge 层测试基础设施

**问题真实性**：**真实，已验证**
- `MacSearchBridge.mm` 和 `MacSearchBridge+Content.mm` 合计约 1,578 行关键并发逻辑。
- 包含 `_shuttingDown` 关闭协调、`_mutationQueue` 串行突变、FSEvents 回调路由、engine 热替换、content indexing 生命周期管理 — 全部零自动化测试。
- Bridge 层历史上产生过多个 Critical/High 级 bug（死锁、feedback loop 等）。

**严重程度判断**：**P1 正确** — 系统性风险，非立即故障，但回归风险极高。

**修复必要性**：**必要**
- Bug 密度最高的区域缺乏测试覆盖，每次修改都有回归风险。P0-1、P0-2、P0-3 的修复都在 Bridge 层，更凸显测试基础设施的必要性。

**方案评价**：
- 使用现有 `test_helpers.h` 框架，不引入外部依赖，复杂度可控。
- 测试用例覆盖 shutdown 协调、engine 热替换、只读模式三个核心场景，针对性强。
- 依赖 P0-1 和 P0-3 完成后才能编写完整测试，依赖关系合理。
- ObjC++ 测试需通过 `clang++ -ObjC++` 编译，需确认构建系统支持。
- **无过度修复风险**：测试补全是技术债偿还，不是功能新增。

**结论**：**通过**

---

### P1-3: `setEngine:` 避免在 main thread 上阻塞等锁

**问题真实性**：**真实，已验证**
- `MacSearchBridge.mm:198,271`：`setEngine:` dispatch 到 main queue，内部获取 `_engineMutex` 的 `unique_lock`。
- `MacSearchBridge.mm:111-114`：`setEngine:` 方法体内 `std::unique_lock<std::shared_mutex> lock(_engineMutex)` 确认获取 exclusive lock。
- 若此时有并发 query 持有 `shared_lock`，main thread 被阻塞直到所有 query 完成。
- 查询百万级记录可达数十毫秒，main thread 阻塞导致 UI 卡顿。

**严重程度判断**：**P1 正确** — setEngine 是低频操作（仅 scan/sync 完成时），但每次触发都可能造成可感知的 UI 卡顿。

**修复必要性**：**必要**
- main thread 不应参与可能阻塞的 lock 竞争，这是 GUI 应用的基本原则。

**方案评价**：
- **第一轮评估要求明确选择方案一（_mutationQueue）**，修复计划应据此调整。
- `_mutationQueue` 已存在于 `MacSearchBridge_Internal.h` 中，是现有基础设施，复用成本低。
- setEngine 是低频操作，不需要 `std::atomic<shared_ptr>` 的 lock-free 性能。
- 避免 `std::atomic<shared_ptr>` 的 C++20 编译器兼容性风险（libc++ 在 Xcode 15 前支持不完整）。
- 改动范围小：仅修改 `setEngine:` 的 dispatch target + UI 通知回调。
- **无过度修复风险**：将 lock 操作从 main thread 移到后台队列是标准做法。

**结论**：**通过** — 前提是删除 `std::atomic<shared_ptr>` 选项，明确选定 `_mutationQueue` 方案。第一轮评估已给出此建议，需确认修复计划已更新。

---

### P2-1: ContentIndexPersistence WAL 改用内存跟踪大小

**问题真实性**：**真实，已验证**
- `ContentIndexPersistence.cpp:46-47`（`appendAdd`）和 `84-85`（`appendRemove`）：每次 append 调用 `fstat(fileno(file_), &st)` 获取文件大小，用于判断是否超过 20MB 限制。
- `IndexWAL.h:80`：`size_t currentSize_ = 0;` 成员变量 + `IndexWAL.cpp:75` 的 `if (currentSize_ >= kMaxWALSize)` — 已有优化模式，使用内存变量跟踪大小。
- 两个 WAL 实现不一致，`ContentIndexPersistence` 未采用 `IndexWAL` 的优化模式。

**严重程度判断**：**P2 正确** — 每次 content indexing 变更多一次 `fstat()` syscall，在高频场景下有可测量但非严重的性能开销。

**修复必要性**：**必要但低优先级**
- 修复简单（添加 `currentSize_` 成员 + 累加），参考 `IndexWAL` 已有实现，无风险。

**方案评价**：
- 直接对标 `IndexWAL` 的实现模式，改动局部且明确。
- `currentSize_` 初始化时从 `fstat` 获取一次，后续通过累加维护，消除重复 syscall。
- **无过度修复风险**：代码级的微优化，消除不必要的 syscall。

**结论**：**通过**

---

### P2-2: `query()` 方法拆分

**问题真实性**：**真实，已验证**
- `SearchEngine.cpp:172-440`：`query()` 方法 268 行，包含 trigram 提取、候选生成、Phase 1 名称匹配、Phase 2 并行路径扫描、优先级排序、结果装配。
- 单一方法职责过重，可读性差，修改风险高。

**严重程度判断**：**P2 正确** — 可维护性问题，非功能性 bug。

**修复必要性**：**必要但低优先级**
- 拆分提升可读性和可测试性，降低后续修改风险。

**方案评价**：
- 拆分为 4 个语义清晰的 private 方法（`extractQueryTrigrams`、`findCandidatesByTrigram`、`filterByPathParallel`、`rankAndSort`），方向正确。
- **需注意实施细节**：`query()` 中大量捕获局部引用用于 `dispatch_apply` block（如 `const auto& records = records_`），拆分后子方法需能正确访问受 `shared_lock` 保护的成员。建议子方法接收引用参数而非直接访问成员。
- `query()` 主体变为 4 个阶段调用的协调者，保持 `shared_lock` 在 `query()` 层持有，子方法不独立获取锁。
- **无过度修复风险**：纯重构，不改变行为。

**结论**：**通过**

---

### P2-3: query 候选位图 thread-local 复用

**问题真实性**：**真实，已验证**
- `SearchEngine.cpp:288`：`std::vector<bool> isCandidate(totalSize, false)` 在 Phase 2（trigram path）内分配，每次进入该路径的查询分配一个与记录总数等大的位图。
- 百万级记录时约 125KB（bit-packed），高频搜索场景下造成不必要的分配压力。
- 注意：该分配仅在 trigram path（有 trigram 候选时）触发，非每次 query。但高频搜索场景下仍然是显著开销。

**严重程度判断**：**P2 正确** — 性能优化，非功能性问题。

**修复必要性**：**可选但有价值**
- `ContentIndex::extractTrigrams` 中已有 thread_local bitmap 的模式（`SearchEngine.cpp` 中可以参考），复用该模式一致性好。

**方案评价**：
- `thread_local static vector<bool>` + `resize` + `fill(false)` 是标准池化模式。
- 每个查询线程常驻一份，总量为 `numThreads * 125KB`。当前 `dispatch_apply` 使用的并发数上限为 32（代码中 `std::min(threadCount, 32u)`），总常驻内存约 4MB，可接受。
- `thread_local` 保证线程安全，无需额外同步。
- **无过度修复风险**：局部优化，改动范围限于 `query()` 方法内。

**结论**：**通过**

---

### P2-4: DirectoryScanner dedup 锁优化

**问题真实性**：**真实但影响有限，已验证**
- `DirectoryScanner.cpp:302-306`：`tryVisitDirectory` 获取 `lock_guard<mutex>` on `dedupMutex_`，用于 inode dedup。
- 但 `tryVisitDirectory` 仅在发现新目录时调用（目录数量远小于文件数量），锁争用频率低。
- `DirectoryScanner.cpp:291-299`：已有 batch-push 优化减少了 `queueMutex_` 争用，说明文件级锁争用已被优化。

**严重程度判断**：**P2 偏高** — 实际瓶颈程度被高估。目录数量远小于文件数量，`dedupMutex_` 不太可能是实际扫描瓶颈。

**修复必要性**：**不必要**
- 收益/复杂度比过低。per-thread dedup set + post-merge 引入 merge 逻辑和潜在的 dedup 遗漏风险。`shared_mutex` 方案在目录插入（写锁）和查询（读锁）频率相当时收益有限。
- 无 profiling 数据证明 `dedupMutex_` 是实际瓶颈。

**方案评价**：
- 两种方案均属于过度优化：
  - 方案一（per-thread + post-merge）大幅增加复杂度，可能引入跨线程 dedup 遗漏 bug。
  - 方案二（shared_mutex）改动成本低但收益同样低，因为读写频率相当。
- **存在过度修复风险**：在无 profiling 数据支撑的情况下优化非瓶颈路径，违反「不过度修改」原则。

**结论**：**不通过，建议移除** — 降级为「观察」项，待未来 profiling 数据表明 `dedupMutex_` 确实是瓶颈（>5% 扫描总时间）后再重新评估。

---

### P2-5: `removeFile` 使用 `lower_bound`

**问题真实性**：**真实，已验证（含偏差说明）**
- `ContentIndex.cpp:293-298`：使用 `std::remove`（非 `std::find`，审计报告表述略有偏差），对 posting list 进行线性扫描删除。
- `ContentIndex.cpp:337-339`：同样使用 `std::remove` 线性扫描。
- posting list 已排序，但删除时未利用有序性。两者（`std::find` 和 `std::remove`）都是 O(N) 线性扫描，根因「未利用有序性」仍然成立。
- 注意：`std::remove` 不仅查找还移动元素，改为 `lower_bound` + erase 可减少查找开销但移动开销不变。实际收益取决于 posting list 长度。

**严重程度判断**：**P2 正确** — posting list 通常较短，实际性能影响有限。

**修复必要性**：**可选但合理**
- 修改简单，算法改进正确，风险低。

**方案评价**：
- `std::lower_bound` + 验证匹配 + `erase` 是正确做法。
- 需注意 `std::remove` 的语义（移除所有匹配项）与 `lower_bound` + `erase`（移除第一个匹配项）不同。如果 posting list 中可能存在重复 docId，需保持 `remove` 语义。需验证 posting list 的唯一性约束。
- **无过度修复风险**：算法级微优化，改动范围限于 2 处。

**结论**：**通过**（但实施时需确认 posting list 的唯一性约束）

---

### P2-6: `StringUtils::toLower` 单元测试

**问题真实性**：**真实，已验证**
- `StringUtils.cpp`（46 行）：ASCII 快速路径 + CoreFoundation `CFStringLowercase` Unicode 回退。
- 仅通过搜索测试间接覆盖，无针对 Unicode 边界（CJK、umlauts、emoji）的专项测试。

**严重程度判断**：**P2 正确** — 测试遗漏，非功能性 bug。

**修复必要性**：**必要**
- `toLower` 是搜索的基础函数，Unicode 大小写转换的回归风险应被测试守护。

**方案评价**：
- 测试用例覆盖合理：ASCII、德语 umlaut、中文（不变）、混合、空字符串、纯数字/符号。
- 使用现有 `test_helpers.h` 框架，在 `tests/test_string_utils.h` 中实现，通过 `test_all.cpp` include，符合项目测试组织规范。
- **无过度修复风险**：纯测试补全。

**结论**：**通过**

---

### P2-7: `exclusionPaths_` 线程安全

**问题真实性**：**真实，已验证**
- `FileSystemWatcher.cpp:26-28`：`setExclusionPaths()` 直接 `std::move` 赋值 `exclusionPaths_`，无任何同步。
- `FileSystemWatcher.cpp:155`：`fseventsCallback` 在 dispatch queue 线程上读取 `exclusionPaths_`。
- 理论上存在 data race：如果在 FSEvents 运行期间调用 `setExclusionPaths()`，concurrent read + write 导致 UB。
- 实践中因使用模式安全（exclusion paths 通常在 `start()` 前设置），但 API 未强制此约束。

**严重程度判断**：**P2 正确** — 理论风险，实践中低概率触发。

**修复必要性**：**合理**
- 修复简单，消除潜在的 data race，使代码正确性不依赖使用模式假设。

**方案评价**：
- 两种方案均可：`lock_guard` + 相同锁保护读写，或 `atomic<shared_ptr<vector<string>>>` lock-free 读。
- 推荐 `lock_guard` 方案（更简单，`setExclusionPaths` 是极低频操作），不需要 lock-free 性能。
- **无过度修复风险**：添加必要的线程同步，改动极小。

**结论**：**通过**

---

### P2-8: `pathEndsWithApp` 参数统一

**问题真实性**：**真实，已验证**
- `MacSearchBridge.mm:569`：`pathEndsWithApp(path)` 使用全路径。
- `MacSearchBridge.mm:667`：`pathEndsWithApp(fileName)` 使用文件名。
- 功能上两种用法都正确（`.app` 目录的路径和文件名都以 `.app` 结尾），但代码一致性差。

**严重程度判断**：**P2 正确（偏低）** — 纯代码质量问题，无功能影响。

**修复必要性**：**可选**
- 统一用法提高可读性，但不修复任何 bug。

**方案评价**：
- 统一使用 `fileName` 更清晰（语义上检查的是文件/目录名是否以 `.app` 结尾，而非路径）。
- 改动极小（一行），无风险。
- **无过度修复风险**。

**结论**：**通过**

---

## 3. 约束检查总结

### 3.1 资源平衡

- **结论**：有条件通过
- **逐项分析**：
  - **P0-2（engine 原子替换）**：短暂 2x 内存峰值（~400MB for 百万级记录），持续毫秒级（旧 engine 在最后一个 shared_ptr 释放后析构）。该行为已存在于 `startScanFrom:` 路径（line 190-198），不是新增开销。**通过**。
  - **P1-1（saveToFile）**：原计划的 full snapshot 方案约 200MB 额外内存，持续整个 I/O 时间（数百 ms 到秒级），**违反资源平衡约束**。需改为方案 A（分批次 lock）或方案 B（最小 snapshot）。
  - **P2-3（thread-local 位图）**：每个线程常驻 ~125KB，最多 32 线程 = ~4MB。**通过**。
  - **其余项**：内存与计算成本无显著变化。**通过**。

### 3.2 复杂度控制

- **结论**：有条件通过
- **逐项分析**：
  - **P2-4（dedup 锁优化）**：per-thread dedup + post-merge 大幅增加复杂度，可能引入 dedup 遗漏 bug。收益/复杂度比过低。**不通过，建议移除**。
  - **P1-3（setEngine）**：第一轮要求明确选定 `_mutationQueue` 方案。若已更新计划删除 `std::atomic<shared_ptr>` 选项，则复杂度可控。**有条件通过**。
  - **P2-2（query 拆分）**：拆分方向正确，需注意子方法对 `shared_lock` 保护成员的访问方式。复杂度合理。**通过**。
  - **其余项**：复杂度可控，均为局部修改。**通过**。

### 3.3 可理解性

- **结论**：通过
- **分析**：
  - 所有修复项均对应审计报告中明确的问题编号，无新增与修复目标无关的功能。
  - P0-3（只读模式）是 instance lock 失败场景的必要降级路径，属于防御性设计。
  - P1-2（Bridge 测试）是测试补全，限定使用现有框架。
  - P2-2（query 拆分）提升可理解性。
  - 无风险。

### 3.4 根因修复

- **结论**：通过
- **逐项验证**：
  - **P0-1**：根因正确 — 终止流程将 compact 视为必须操作是设计错误。WAL flush 即可保证增量安全。
  - **P0-2**：根因正确 — 问题在于在活跃 engine 上做 exclusive 操作。修复对齐到已有正确模式。
  - **P0-3**：根因正确 — 缺乏降级路径导致 warn-and-continue。只读模式是系统性修复。
  - **P1-1**：根因正确 — lock 持有时间应与数据访问时间成正比而非 I/O 时间。
  - **P2-1**：根因正确 — 两个 WAL 实现不一致，应对齐到已有优化模式。
  - **P2-5**：根因正确 — 未利用有序性（审计报告说 `std::find`，实际为 `std::remove`，但两者都是线性扫描，根因成立）。
  - **其余项**：根因识别均准确。
  - 无表层补丁行为。

---

## 4. 最终建议

### 保留（12 项）

以下修复项通过评估，可按原计划执行：

| 项目 | 说明 |
|------|------|
| P0-1 | 重构 `prepareForTermination` 终止流程 |
| P0-2 | Background sync 改用 engine 原子替换 |
| P0-3 | Instance lock 失败时阻止写入 |
| P1-2 | 建立 Bridge 层测试基础设施 |
| P1-3 | `setEngine:` 移到 `_mutationQueue`（明确选定方案一） |
| P2-1 | ContentIndexPersistence WAL 改用内存跟踪大小 |
| P2-2 | `query()` 方法拆分 |
| P2-3 | query 候选位图 thread-local 复用 |
| P2-5 | `removeFile` 使用 `lower_bound` |
| P2-6 | `StringUtils::toLower` 单元测试 |
| P2-7 | `exclusionPaths_` 线程安全 |
| P2-8 | `pathEndsWithApp` 参数统一 |

### 调整（1 项）

| 项目 | 调整内容 |
|------|----------|
| P1-1 | 将 full snapshot 方案替换为分批次 lock 方案（方案 A，推荐）或最小 snapshot 方案（方案 B）。方案 A：将 `shared_lock` 从覆盖整个 `saveToFile` 缩小到分批次数据读取（每批 ~1000 条记录），读取后释放 lock，写入磁盘，lock 持有时间从秒级降到毫秒级，内存增量接近零。方案 B：仅 snapshot `records_`、`pathTable_`、`pathIndices_` 三者，排除 trigram index 等非持久化数据，内存增量约 50-100MB。 |

### 移除（1 项）

| 项目 | 移除原因 |
|------|----------|
| P2-4 | 收益/复杂度比过低。`tryVisitDirectory` 仅在发现目录时调用（目录数量远小于文件数量），且已有 batch-push 优化。per-thread dedup 方案引入 merge 逻辑和 dedup 遗漏风险，`shared_mutex` 方案读写频率相当收益有限。无 profiling 数据支撑。建议降级为「观察」项，待未来 profiling 数据表明 `dedupMutex_` 确实是瓶颈后再重新评估。 |

---

## 5. 与第一轮评估的差异

| 维度 | 第一轮 | 第二轮 | 变化说明 |
|------|--------|--------|----------|
| P1-1 | 不通过，提出方案 A/B | 有条件通过，推荐方案 A | 第二轮更明确推荐方案 A（分批次 lock），方案 B 作为备选 |
| P1-3 | 需澄清，两方案未选定 | 通过（选定 _mutationQueue） | 与第一轮建议一致，确认选定方案一 |
| P2-4 | 不通过，建议降级 | 不通过，建议移除 | 与第一轮结论一致 |
| 代码验证深度 | 部分验证 | 全量验证（~30 文件） | 第二轮对全部 15 个审计问题逐一在源码中验证 |
| 审计偏差 | 提及 L-02 std::find vs std::remove | 确认并详述 | std::remove 同为线性扫描，根因成立 |
