# 审计报告

## 1. 执行摘要

- **审计范围**：MacEverything 全项目 — Core 引擎(C++20, 23文件/~3,274行)、Bridge 桥接层(ObjC++, 4文件/~1,578行)、App UI 层(SwiftUI, 14文件/~1,880行)、Tests(40文件/~5,673行)
- **审计维度**：性能热点、并发安全、内存管理、I/O 效率、逻辑正确性、错误处理、测试覆盖
- **关键风险概览**：发现 4 个 Critical/High 级别问题，6 个 Medium 级别问题，5 个 Low 级别问题。核心风险集中在「终止流程超时」「查询阶段性阻塞」「WAL 并发写入」和「Bridge 层零测试覆盖」。

---

## 2. 问题清单（按严重级别排序）

---

### [Critical] C-01: `prepareForTermination` 可能超出 macOS 终止超时

- **证据**：`MacSearchBridge.mm:475-509`  
  - 先等待 content indexing 完成（semaphore wait 最多 5 秒，line 492）  
  - 再执行 `persistence->compact(lastEventId)`（写入全量 filename 索引，百万级记录可达数百 ms）  
  - 再执行 `contentPersistence->compact()`（写入全量 content 索引）  
  - 总耗时最坏情况可达 ~7-10 秒  
- **根因**：终止流程设计为同步阻塞式，所有清理/持久化操作串行执行在 main thread 上，无超时保护和优先级裁剪  
- **影响**：macOS 对 `applicationWillTerminate:` 的系统超时通常为 ~5 秒；超时后进程被 SIGKILL，导致 WAL 未 flush、索引状态丢失，下次启动需全量重建  
- **修复优先级**：P0 — 直接影响数据持久化可靠性

---

### [High] H-01: `loadRecords()` 在后台同步时长时间持有 exclusive lock，阻塞所有查询

- **证据**：`MacSearchBridge.mm:417`、`SearchEngine.cpp` 中 `loadRecords()` 获取 `unique_lock<shared_mutex>`  
  - `loadRecords` 需处理数百万条记录（清空旧数据、重建 trigram 索引、PathTable），耗时可达数百毫秒到秒级  
  - 期间所有 `query()` 调用因 `shared_lock` 被阻塞  
- **根因**：background sync 直接在已有 engine 上原地重建，而非构建新 engine 后原子替换  
- **影响**：用户在后台同步期间体验到搜索卡顿/无响应  
- **修复优先级**：P0 — 影响用户交互体验

---

### [High] H-02: Instance lock 失败后继续运行，可能导致 WAL 双写腐败

- **证据**：`MacSearchBridge.mm:240-242`  
  ```objc
  if (!_instanceLock.tryLock(lockPath)) {
      LOG_WARN("Bridge", "Another instance may be running — proceeding with caution");
  }
  ```
- **根因**：lock 失败被视为非致命警告而非错误，缺乏用户通知和保护措施  
- **影响**：两个进程同时写同一个 WAL 文件，CRC32 校验可能通过（各自写入完整条目），但语义状态不一致，导致索引腐败  
- **修复优先级**：P0 — 数据完整性风险

---

### [High] H-03: Bridge 层（1,578行关键并发逻辑）零自动化测试覆盖

- **证据**：`MacSearchBridge.mm` 和 `MacSearchBridge+Content.mm` 包含：  
  - `_shuttingDown` 关闭协调逻辑（曾出过 2 个 Critical 级死锁 bug）  
  - `_mutationQueue` 串行突变队列  
  - FSEvents 回调路由和 rescan debounce  
  - engine 热替换（`setEngine:`）  
  - 内容索引生命周期管理  
  - 以上均无任何自动化测试  
- **根因**：Objective-C++ 代码难以进行单元测试，且项目缺乏 Bridge 层的测试基础设施  
- **影响**：Bridge 层是 bug 密度最高的区域（历史 Critical/High bug 集中区），无测试覆盖意味着回归风险极高  
- **修复优先级**：P1 — 系统性风险，但非立即故障

---

### [Medium] M-01: `saveToFile()` 持有 `shared_lock` 全程写磁盘，阻塞所有写操作

- **证据**：`SearchEnginePersistence.cpp` 中 `saveToFile()` 获取 `shared_lock<shared_mutex>` 并持有直到文件写入完成  
- **根因**：持久化写入在 lock 内执行，而非先 snapshot 数据再在 lock 外写文件  
- **影响**：大型索引的持久化期间（可达数百 ms），所有 `addRecord`/`removeByPath` 等写操作被阻塞  
- **修复优先级**：P1

---

### [Medium] M-02: `setEngine:` 在 main thread 上获取 `unique_lock`，可能被查询阻塞

- **证据**：`MacSearchBridge.mm:198,271` — `setEngine:` dispatch 到 main queue，内部获取 `_engineMutex` unique_lock  
  - 如果此时有并发查询持有 `shared_lock`，main thread 会被阻塞直到查询完成  
  - 查询对百万级记录可达数十 ms  
- **根因**：engine 替换操作放在 main thread 上，且使用阻塞式 lock  
- **影响**：UI 主线程间歇性卡顿  
- **修复优先级**：P1

---

### [Medium] M-03: `ContentIndexPersistence` WAL 每次 append 调用 `fstat()` 检查文件大小

- **证据**：`ContentIndexPersistence.cpp` — 每次 `appendAddEntry`/`appendRemoveEntry` 调用 `fstat()` 检查是否超过 20MB 限制  
  - 对比 `IndexWAL` 使用内存变量 `currentSize_` 跟踪大小，避免了 syscall  
- **根因**：两个 WAL 实现不一致，`ContentIndexPersistence` 未采用 `IndexWAL` 的优化模式  
- **影响**：每次内容索引变更多一次 `fstat()` syscall；在高频内容索引场景下有可测量的性能开销  
- **修复优先级**：P2

---

### [Medium] M-04: `query()` 方法 268 行，职责过重

- **证据**：`SearchEngine.cpp` — `query()` 单一方法包含：trigram 提取、候选生成、Phase 1 名称匹配、Phase 2 并行路径扫描、优先级排序、结果装配  
- **根因**：功能演进过程中未及时拆分  
- **影响**：可读性差、难以独立测试各阶段、修改风险高  
- **修复优先级**：P2

---

### [Medium] M-05: `dedupMutex_` 在 DirectoryScanner 中成为多线程竞争瓶颈

- **证据**：`DirectoryScanner.cpp` — 每个工作线程在发现目录时都需要获取 `dedupMutex_` 查询/插入 `InodeKey` dedup set  
- **根因**：使用全局互斥锁保护 dedup set，而非 per-thread dedup + post-merge 或 concurrent hash set  
- **影响**：在目录密集的文件系统上（如 `node_modules` 嵌套），线程利用率下降  
- **修复优先级**：P2

---

### [Medium] M-06: 每次 query 分配 `vector<bool>` 用于候选跟踪

- **证据**：`SearchEngine.cpp` — 每次 `query()` 分配一个与记录总数等大的 `vector<bool>` 作为候选位图  
- **根因**：无候选位图池化或 thread-local 复用  
- **影响**：百万级记录时每次查询分配约 125KB（bit-packed），在高频搜索场景下造成不必要的分配压力  
- **修复优先级**：P2

---

### [Low] L-01: `exclusionPaths_` 在 `FileSystemWatcher` 中无线程同步

- **证据**：`FileSystemWatcher.cpp:26-28` — `setExclusionPaths()` 写入 `exclusionPaths_` 无锁；`fseventsCallback` 在 dispatch queue 线程上读取  
- **根因**：假设 exclusion paths 仅在 `start()` 前设置，但 API 未强制此约束  
- **影响**：理论上存在 data race，实践中因使用模式安全  
- **修复优先级**：P2

---

### [Low] L-02: `removeFile` 在 ContentIndex 中使用 `std::find` 而非 `std::lower_bound`

- **证据**：`ContentIndex.cpp:293-298,337-339` — posting list 已排序，但删除时使用线性查找  
- **根因**：实现时未利用有序性  
- **影响**：删除操作时间复杂度从 O(log N) 退化为 O(N)，但 posting list 通常较短，实际影响有限  
- **修复优先级**：P2

---

### [Low] L-03: Logger mutex 在控制台输出期间持有

- **证据**：`Logger.cpp:54-61` — 在 `mutex_` 保护下同时写文件和 stdout/stderr  
- **根因**：简化实现，未将控制台输出移到 lock 外  
- **影响**：控制台 I/O 阻塞时（管道满、终端缓冲区满）会拖慢所有日志调用方  
- **修复优先级**：P2

---

### [Low] L-04: `StringUtils::toLower` 无直接单元测试

- **证据**：`StringUtils.cpp`（45行）使用 CoreFoundation `CFStringLowercase` 处理 Unicode  
  - 仅通过搜索测试间接覆盖，无针对 Unicode 边界（CJK、umlauts、emoji）的专项测试  
- **根因**：测试遗漏  
- **影响**：Unicode 大小写转换的回归风险未被守护  
- **修复优先级**：P2

---

### [Low] L-05: `pathEndsWithApp` 参数不一致（fileName vs fullPath）

- **证据**：`MacSearchBridge.mm:569` 使用 `pathEndsWithApp(path)`（全路径），`MacSearchBridge.mm:667` 使用 `pathEndsWithApp(fileName)`（文件名）  
- **根因**：两处代码在不同上下文中编写，未统一  
- **影响**：功能上正确（两种情况下 `.app` 目录的路径/文件名都以 `.app` 结尾），但代码一致性差  
- **修复优先级**：P2

---

## 3. 总体治理建议

### 3.1 架构层面
- **终止流程重构**：将 `prepareForTermination` 改为异步化 + 超时裁剪，优先保存 WAL（快），降级放弃全量 compact（慢）
- **Engine 热替换模式推广**：background sync 应采用 `startScanFrom:` 的模式（构建新 engine → 原子替换），而非原地 `loadRecords`

### 3.2 测试层面
- **Bridge 层测试基础设施**：建立可脱离 UI 运行的 Bridge 层测试，至少覆盖 shutdown 协调、engine 替换、FSEvents 回调路由
- **StringUtils 单元测试**：添加 Unicode 边界测试（CJK、德语 umlauts、组合字符）

### 3.3 性能层面
- **消除不必要的 syscall**：ContentIndexPersistence WAL 改用内存跟踪文件大小
- **减少 lock 持有时间**：saveToFile 先 snapshot 再写文件；Logger 控制台输出移到 lock 外
- **池化分配**：query 候选位图使用 thread-local 复用

### 3.4 数据完整性
- **Instance lock 失败应阻止启动**：而非 warn-and-continue，或至少通知用户并禁用写操作
